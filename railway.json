{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "NODE_TLS_REJECT_UNAUTHORIZED=0 npx ts-node scripts/$([ \"$BOT_TYPE\" = \"task\" ] && echo task_bot.ts || echo bot_server.ts)",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
